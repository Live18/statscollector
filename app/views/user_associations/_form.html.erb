<%= form_with(model: user_association, local: true) do |form| %>
  <% if user_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_association.errors.count, "error") %> prohibited this user_association from being saved:</h2>

      <ul>
      <% user_association.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.number_field :team_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
